import 'package:flutter/material.dart';

class GiveHomeworkPage extends StatelessWidget {
  const GiveHomeworkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ödev Verme Sayfası'),
      ),
    );
  }
}